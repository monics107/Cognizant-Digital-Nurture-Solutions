// jQuery Click Event

$("#registerBtn").click(function () {

    alert(
        "Registration Successful!"
    );

    // Fade Out Card

    $("#eventCard").fadeOut(2000);

    // Fade In Again

    $("#eventCard").fadeIn(2000);

});

// Framework Information

console.log(

    "Frameworks like React and Vue help build dynamic and reusable UI components."

);